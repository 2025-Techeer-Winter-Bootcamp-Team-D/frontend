# 프로덕션 환경 설정

# MSW 비활성화 (프로덕션에서는 실제 API 사용)
VITE_MSW_ENABLED=false

# API Base URL (배포 시 실제 도메인으로 변경)
VITE_API_BASE_URL=https://quasa.info

# WebSocket URL (배포 시 실제 도메인으로 변경, HTTPS 환경에서는 wss:// 사용)
VITE_WS_URL=wss://quasa.info/ws/stock/
